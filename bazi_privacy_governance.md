# 八字／流年私密資料治理規格

## 目的與範圍

本規格處理使用者主動選擇保存的八字資料。既有「排出四柱與流年」維持**暫時運算、預設不保存**；只有已登入使用者另行勾選「同意加密私密保存」並按下保存時，系統才建立資料列。

> 生辰日期、出生時間、時區與日後可能加入的出生地，均屬敏感輸入。它們不得出現在伺服器日誌、分享快照、公開 URL、匯出資料或未加密的資料表欄位。

## 資料模型與生命週期

| 資料表 | 保存內容 | 敏感性 | 刪除規則 |
| --- | --- | --- | --- |
| `birthProfiles` | 使用者自訂名稱、加密後的生辰／時區／出生地 JSON、加密版本、明示同意版本與時間 | 高 | 刪除 profile 時永久刪除，並級聯刪除所有排盤與年度閱讀 |
| `baziCharts` | 四柱、藏干、十神、可見五行、計算慣例與無生辰明文的結構化結果 | 中 | 隨 profile 級聯刪除；可單獨刪除單筆 chart |
| `annualReadings` | 指定流年、結構化流年結果與受限 AI 研究解說 | 中 | 隨 chart／profile 級聯刪除；可單獨刪除 |
| `sensitiveDataConsents` | 同意目的、版本、時間與撤銷時間；不含生辰原文 | 中 | profile 刪除時級聯刪除；撤銷後不得再保存或讀取資料 |

每一筆資料均以 `userId` 授權。所有讀取、更新、刪除程序都必須使用 `protectedProcedure`，並以 `ctx.user.id` 限制資料列，不接受前端傳來的擁有者識別值。

## 加密與最小保存

| 控制項 | 規則 |
| --- | --- |
| 加密演算法 | AES-256-GCM；每筆 profile 使用隨機 IV，並保存驗證標籤與加密版本 |
| 金鑰來源 | 伺服器端 `BAZI_DATA_ENCRYPTION_KEY`；僅以環境祕密注入，禁止送往前端或存入資料庫 |
| 加密內容 | 日期、時間、時區、可選出生地及日後新增的敏感輸入，封裝為單一 JSON payload 後加密 |
| 非敏感欄位 | profile 名稱、使用者 ID、加密中繼資料、同意紀錄及已計算的結構化盤面可分欄保存 |
| 金鑰輪替 | 以 `encryptionVersion` 記錄；輪替時重新加密仍有效的 profile，不改寫歷史同意紀錄 |

敏感資料只在使用者要求「從已保存 profile 重算」時，於伺服器記憶體短暫解密並立即傳入規則引擎；不得快取、不得加進 AI 提示或回傳前端。

## 分享與匯出遮罩

分享快照採取**欄位白名單**，而非黑名單。八字公開快照只允許：四柱、目標流年、結構化計算慣例、眉批與受限研究解說。不得含有出生日期、出生時間、時區、出生地、加密 payload、IV、驗證標籤、profile ID、同意紀錄或私人觀察。

所有既有通用分享 API 也須先套用遞迴敏感鍵遮罩，避免日後其他介面意外傳入 `birthDate`、`birthTime`、`location`、`timezone`、`encryptedPayload` 等欄位。分享頁固定顯示「出生資料已遮罩；僅供結構研讀」。

## 刪除、撤銷與可見性

使用者可刪除單一已保存 profile，系統應級聯永久刪除其 charts、annual readings 與 consent records。使用者可刪除單一 chart；該 chart 的 annual readings 也應一併刪除。撤銷保存同意時，系統先執行相同永久刪除，再記錄一筆非敏感的撤銷事件（如有必要）。

前端應明確區分「清除此頁暫時結果」與「永久刪除已保存資料」；後者需二次確認。任何分享連結只包含已遮罩快照，並保有既有的撤銷與過期控制。

## 驗收條件

1. 未勾選保存同意時，資料庫不得新增 profile、chart、annual reading 或 consent。
2. 已保存資料的資料表或快照中，不得搜尋到出生日期、時間、時區或出生地明文。
3. 使用者 A 無法列出、讀取、刪除或分享使用者 B 的 profile 或 chart。
4. 刪除 profile 後，所有關聯資料均不存在；再讀取須回傳找不到資料。
5. 分享 payload 和匯出 payload 均不含敏感欄位，並顯示遮罩揭露。
6. 加密／解密、遮罩、授權、級聯刪除與分享白名單均有單元測試覆蓋。
