# 八字大運與排盤慣例：B 階段來源查核紀錄

## 目前可確認的依賴能力

本專案已使用 `lunar-javascript@1.7.7`。官方專案說明將其定位為無第三方依賴的公曆、農曆與相關曆法工具，並列示八字、五行、十神與節氣等功能；本階段不得把套件存在誤述為任一命理學派的唯一正確規則。[1]

官方專案目前的版本紀錄顯示 `v1.7.7` 已發布；本專案的既有計算依據卡會保留此引擎版本，未來大運輸出也必須回顯規則版本。[1]

## 待實作前確認的版本化規則

| 議題 | 預定處理方式 | 目前狀態 |
|---|---|---|
| 順逆行 | 不預設推定；要求使用者明示選擇或另列採用學派規則與性別／年干依據 | 待釐清 API 與測試向量 |
| 起運基準 | 將「出生時刻至相鄰節氣的時間差」與換算公式作為可見參數，不輸出事件預測 | 待確認 API 與換算精度 |
| 十年大運柱 | 依月柱干支順推或逆推，輸出為結構時間軸 | 待以可重現案例測試 |
| 子初／子正 | 延續既有 23:00／00:00 日界選擇，並獨立寫入每張 chart 的 conventions | 已有基礎實作 |
| 真太陽時 | 保持預設「未套用」；除非取得出生地座標與可稽核演算法，絕不靜默校正 | 已有揭露，待設計選用規格 |

## 已核對的套件規則契約

在本專案鎖定的 `lunar-javascript@1.7.7` 原始碼中，`EightChar.getYun(gender, sect)` 以 `gender=1` 表示男、`gender=0` 表示女；順逆行由「年干陰陽」與性別共同決定。套件的 `sect=2` 以分鐘差換算起運年、月、日、時，`sect=1` 則依時辰差換算。兩者都應作為明示的「起運換算口徑」，不得靜默替換。[1]

`getYun()` 回傳起運年／月／日／時、起運公曆日與順逆行旗標；`getDaYun(n)` 預設產生十段，第一筆是出生至起運前的過渡區段，後續每筆為十年一柱，柱位從月柱干支按順逆行推移。B 階段 UI 必須將前置過渡區段清楚標為「起運前」，避免誤當作十年大運柱。[1]

官方 API 文件提供 Lunar 相關類別索引；本階段實際採用的 `getYun` 與 `getDaYun` 行為，另以專案鎖定版本 `v1.7.7` 的官方原始碼與內建 Yun 測試交叉核對。這可確保文件索引不完整時，仍以實際依賴版本為準。[1] [2]

## B 階段最小可交付規格

* 對每一次即時計算顯示「大運選擇」：性別／順逆行依據、起運換算口徑（套件 sect 1 或 2）與既有子初／子正換日。
* 預設提供第一至八步十年大運及指定流年所在區段；不預設開啟小運與流月，避免超出本階段的可檢查範圍。
* 每一張大運卡需回顯開始／結束西元年、年齡區間、干支、順逆行及起運日；並固定揭露民用時間、未採真太陽時與引擎版本。
* 對於未提供性別的資料，不計算大運，而以可讀說明要求選擇；不從姓名、帳號或其他資料推定。

## 安全與產品邊界

大運僅呈現傳統排盤中的時間標記與計算慣例，不應轉換為個人吉凶、事件、醫療、法律、投資或其他重大決策預測。所有 AI 文字只得解釋已計算的結構、慣例差異、研究問題與限制。

## 來源

[1] [6tail/lunar-javascript 官方專案與 v1.7.7 原始碼](https://github.com/6tail/lunar-javascript)
[2] [6tail Lunar API 文件](https://6tail.cn/calendar/api.html)
