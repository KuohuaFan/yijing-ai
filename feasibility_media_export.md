# 《易經 AI》多媒體與交付功能可行性核實報告

**核實結論：三項調整均可行。**其中，無框輸入屬於前端樣式調整，可直接進行；圖片、影片與語音附件需要新增檔案中繼資料、上傳程序與存取權限；分享、儲存、下載與 PDF 則應以「私密紀錄—可控分享—結構化匯出」三層交付。這些變更可完全避開三錢起卦、六爻由下至上的累積、變爻判定與本卦／之卦計算程式，因此不會改動既有卦象生成步驟。

## 一、逐項可行性

| 項目 | 可行性 | 建議作法 | 對六爻起卦的影響 |
|---|---|---|---|
| 對話輸入去外框 | 可立即實作 | 對話與起卦問題輸入改為透明、無外框；保留游標、可見焦點與底部細線或柔和底色，避免失去鍵盤操作辨識。 | **無**；僅改輸入元件樣式。 |
| 圖片上傳 | 可行 | 以隱藏式檔案選取器與附件圖示開啟；前端預覽，伺服器驗證 MIME、大小與使用者身分後存入 S3，再將附件連到對話或反思紀錄。 | **無**；附件是對話的上下文，不參與起卦計算。 |
| 語音上傳／錄音 | 可行 | 先上傳音檔，再執行轉錄；將文字稿、時間戳與原音檔關聯到同一筆對話。內建轉錄支援 webm、mp3、wav、ogg、m4a，單檔轉錄上限為 16 MB。 | **無**；語音僅轉為可選擇送入問易的文字材料。 |
| 影片上傳 | 可行，但分兩階段 | 第一期提供 MP4／WebM 上傳、預覽、下載與關聯保存；第二期才提供抽取音軌、轉錄、字幕與畫面語義分析。 | **無**；影片不得自動改寫卦象或啟動預測性解讀。 |
| 分享 | 可行 | 行動裝置優先呼叫原生分享；不支援時退回複製連結／文字。公開分享必須以唯讀、隨機權杖、可撤銷與可設定有效期的頁面實作。 | **無**；只分享既有結果快照。 |
| 儲存 | 已有基礎，可擴充 | 登入使用者保存到個人書架；未登入則保留於目前瀏覽器。附件與對話需新增關聯資料表，以便跨裝置同步與刪除管理。 | **無**；沿用既有起卦紀錄保存。 |
| 下載 | 可行 | 立即提供 Markdown、JSON 與原附件下載；下載內容含問題、六爻、本卦、變爻、之卦、引文錨點、AI 導讀與附件清單。 | **無**；輸出的是已生成的不可變更快照。 |
| PDF 匯出 | 可行，建議第二期 | 以伺服器端結構化 PDF 產生器生成「研讀／起卦報告」；需選擇具合法授權的繁中字型，且不應以瀏覽器截圖取代文字型 PDF。 | **無**；以既有紀錄輸出。 |

## 二、輸入區的設計處理

您提供的畫面顯示，起卦對話框中的問題欄仍有完整外框，而主 Chat 輸入列已較接近沉浸式樣式。改版會統一採用「**無外框、非無狀態**」的原則：移除四邊框與卡片陰影，但保留焦點時的淡金色底線、游標與附件狀態，避免行動版使用者不知道目前正在輸入的位置。

輸入列建議由左至右設置附件按鍵、文字區、錄音按鍵與送出按鍵。附件按鍵開啟同一個底部選單，清楚分為「照片／圖片」、「影片」、「檔案」與「語音錄製」；已選附件先顯示為可移除的縮圖或檔案列，再決定是否送入問易。瀏覽器可透過使用者主動選擇的檔案讀取內容，並可採用自訂介面觸發檔案選取器。[1]

## 三、多媒體附件的安全與資料設計

媒體資料不應直接放入資料庫欄位，而應儲存於既有 S3 檔案層；資料庫只保存附件的擁有者、檔名、MIME 類型、大小、儲存鍵、對話／起卦紀錄關聯、建立時間與處理狀態。每次下載或播放都應透過受控 URL，而非裸露永久網址。

第一期宜設定明確白名單與配額，例如圖片為 PNG、JPEG、WebP；影片為 MP4、WebM；語音為 webm、mp3、wav、ogg、m4a。伺服器端必須再次驗證 MIME 與大小，不能只相信瀏覽器宣告的副檔名。語音轉錄只能在使用者明確按下「轉成文字」後執行，並在附件旁顯示「原音／文字稿」與刪除控制。影片第一期只作為附件與回看材料；若要對影片內容生成 AI 意義分析，必須另設用途告知、影格／音軌處理成本與敏感內容規則。

## 四、分享與交付的分層設計

| 層級 | 功能 | 權限與隱私規則 | 交付內容 |
|---|---|---|---|
| 私人保存 | 儲存至書架、延續對話、附件關聯 | 僅本人；未登入僅限目前瀏覽器。 | 完整內容、附件、草稿與反思。 |
| 受控分享 | 產生唯讀分享連結、原生分享、複製連結 | 預設不公開；可設定到期、撤銷與是否包含附件。 | 只讀的研讀／起卦快照。 |
| 下載 | Markdown、JSON、原附件 | 使用者主動下載；下載檔標示版本與匯出日期。 | 結構化資料與可再利用文字。 |
| 正式交付 | PDF 研讀報告 | 僅已保存紀錄；產生前可選擇是否含提問、附件與 AI 導讀。 | 封面、卦象、古書引文、AI 六段導讀、反思與來源。 |

原生分享功能可在支援的 HTTPS 瀏覽器中呼叫裝置分享面板，分享文字、連結與部分檔案；但並非所有常用瀏覽器都完整支援，因此網站必須備有複製連結與下載回退方案。[2] 一般檔案下載則可利用瀏覽器的 `download` 屬性指定建議檔名，但使用者裝置仍可自行決定是否真的下載與如何命名。[3]

## 五、明確不變更的卦象生成範圍

本次改版應採用「附件／交付層」與「卦象運算層」分離原則。三錢法的每次投擲、6／7／8／9 判定、初至上爻的順序、本卦與之卦轉換、變爻標示，以及結果寫入歷程的時點均維持現狀。附件只會附著於使用者問題、聊天訊息或完成後的起卦紀錄；不會作為亂數來源、不會改動爻值，也不會取代數位起卦的既有治理聲明。

## 六、建議的實作次序

**第一期**可先完成無框輸入、圖片／語音／影片檔案上傳與預覽、對話／起卦附件保存、複製連結、Markdown／JSON 下載，並保持起卦模組不動。**第二期**再完成登入後附件同步、受控分享頁、語音轉錄、PDF 匯出與附件存取期限。**第三期**才評估影片字幕、影音內容分析、多人協作與可編輯報告模板。

> 建議先做「附件保存＋下載」而不是立即做「自動解讀影片」。前者能安全地增強研讀與反思的材料管理；後者涉及內容處理成本、隱私告知與 AI 分析邊界，應另立規格。

## References

[1]: https://developer.mozilla.org/en-US/docs/Web/API/File_API/Using_files_from_web_applications "MDN：Using files from web applications"
[2]: https://developer.mozilla.org/en-US/docs/Web/API/Web_Share_API "MDN：Web Share API"
[3]: https://developer.mozilla.org/en-US/docs/Web/API/HTMLAnchorElement/download "MDN：HTMLAnchorElement.download"
